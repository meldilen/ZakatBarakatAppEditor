image: google/dart

stages:
  - build
  - deploy

cache:
  paths:
    - .pub-cache

before_script:
  - flutter channel stable
  - flutter upgrade
  - flutter config --enable-web

build:
  stage: build
  script:
    - flutter pub get
    - flutter build web
  artifacts:
    paths:
      - build/web
    expire_in: 1 week

pages:
  stage: deploy
  script:
    - mv build/web public
  artifacts:
    paths:
      - public